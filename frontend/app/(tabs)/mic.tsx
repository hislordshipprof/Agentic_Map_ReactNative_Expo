/**
 * Mic tab placeholder - never actually rendered.
 * The FAB intercepts the tab press and navigates to /voice-assistant.
 */

import { View } from 'react-native';

export default function MicPlaceholder(): JSX.Element {
  return <View />;
}
